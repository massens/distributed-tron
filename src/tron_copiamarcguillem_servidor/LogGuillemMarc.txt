//Marc
Carpetas client i servidor i utils pels comms

El Controlador_Client ha de fer el mateix, contolar la entrada del Client. Canvio el métode updateDirecció per Update simplement. El model.dibuixaLineas() que abans feia el Controlador, ara el fara el Model quan rebi informació del servidor.

Aviam que et sembla aquest cicle.

Usuari fa coses --> Controlador ho reb i crida model.update() -->
--> update() envia canvis al servidor a traves del Client Stream -->
-->El servidor procesa i etc, i llavors fa broadcast a tothom -->
--> Aquest broadcast ho reb el ControladorComunicacions (Com en el xat)-->
--> El controladorCounicacions cridara model.updateDirection() que sera el metode que actualizara les direccions i cridara dibuixaLineas() que pinten la pantall (a traves de cridar la vista).


